## Example environment configuration for the Sorgulen backend

# Port to listen on. Defaults to 10000 when unset.
PORT=10000

# Node environment (production/development)
NODE_ENV=production

# MongoDB connection string including credentials and database name
MONGODB_URI=mongodb+srv://username:password@cluster0.mongodb.net/sorgulen?retryWrites=true&w=majority

# JWT secret and expiration. Use a long, random string for JWT_SECRET.
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRES=12h

# SMTP settings for nodemailer. When using Gmail set secure to true
# and port to 465.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-gmail-user@gmail.com
SMTP_PASS=your-app-password

# Default from address for outgoing mail
MAIL_FROM="Sørgulen Industriservice <no-reply@sorgulen.no>"

# Allowed origins for CORS (comma separated list)
NETLIFY_ORIGIN=https://sorgulen.no,https://www.sorgulen.no

# Base URL where the backend is served (used in emails)
BASE_URL=https://api.sorgulen.no

# Path where AdminJS should be mounted
ADMIN_BASE_URL=/admin

# Secret for signing AdminJS session cookies (should be long and random)
ADMIN_COOKIE_SECRET=change_me_to_another_long_random_string

# Seed owner credentials used to bootstrap the first admin account
SEED_OWNER_EMAIL=owner@sorgulen.no
SEED_OWNER_PASSWORD=temporary-password

# Company email address that should receive internal order alerts
COMPANY_EMAIL=alerts@sorgulen.no